# Next.js Official Tutorial Notes

Tutorial: Next.js App Router Dashboard  
Repository: посилання на GitHub репозиторій

## 01. Ініціалізація проєкту та запуск локального сервера

Скріншоти:

- [01-create-project-console](./screenshots/01-create-project-console.png)

Що було зроблено:

- Було відкрито вже створений проєкт Next.js.
- Встановлено або перевірено залежності проєкту.
- Запущено локальний сервер розробки за допомогою команди:

```bash
pnpm dev
```

- Проєкт було відкрито у браузері за адресою: http://localhost:3000

## 02. CSS Styling

Скріншоти:

- [02-css-browser](./screenshots/2-css-browser.png)
- [03-css-code](./screenshots/03-css-code.png)

Що було зроблено:

- Було пройдено другий розділ офіційного туторіалу Next.js — **CSS Styling**.
- Ознайомилась зі способами додавання стилів у Next.js.
- Перевірила, як глобальні стилі впливають на весь застосунок.
- Переглянула використання CSS Modules для стилізації окремих компонентів.
- Запустила локальний сервер і перевірила зміни у браузері.

Спостереження:

- У Next.js можна використовувати різні підходи до стилізації: глобальні CSS-файли, CSS Modules та utility-класи.
- Глобальні стилі зручно використовувати для базового оформлення всього застосунку.
- CSS Modules допомагають уникнути конфліктів назв класів, оскільки стилі застосовуються локально до конкретного компонента.
- Зміни у стилях швидко відображаються у браузері під час роботи локального сервера.

Питання та роздуми:

- У яких випадках краще використовувати CSS Modules, а коли Tailwind CSS?
- Чи зручно підтримувати великий проєкт, якщо використовувати кілька підходів до стилізації одночасно?
- Мені здається корисним, що Next.js дозволяє обирати спосіб стилізації залежно від потреб проєкту.

## 03. Оптимізація шрифтів та зображень

Скріншоти:

- [04-fonts-code](./screenshots/04-fonts-code.png)
- [05-homepage-browser](./screenshots/05-homepage-browser.png)

Що було зроблено:

- Було пройдено третій розділ офіційного туторіалу Next.js — **Optimizing Fonts and Images**.
- Додано та використано шрифт `Lusitana` через вбудований механізм `next/font`.
- Підключено шрифт до тексту на головній сторінці.
- Ознайомилась з оптимізацією зображень у Next.js.
- Переглянула, як компонент `Image` допомагає автоматично оптимізувати зображення.

Спостереження:

- Next.js має вбудовану підтримку оптимізації шрифтів, тому не потрібно вручну підключати їх через зовнішні CSS-посилання.
- Використання `next/font` допомагає покращити продуктивність і уникнути зайвого завантаження шрифтів.
- Компонент `Image` є зручним, тому що дозволяє оптимізувати розмір, якість і завантаження зображень.
- Для коректного використання зображень потрібно вказувати `width`, `height` або інші параметри, щоб сторінка не “стрибала” під час завантаження.

Питання та роздуми:

- Цікаво, наскільки сильно `next/font` впливає на швидкість завантаження сторінки у великих проєктах.
- Варто краще розібратися, коли краще використовувати звичайний тег `<img>`, а коли компонент `Image`.
- Мені здається корисним, що Next.js одразу пропонує інструменти для оптимізації продуктивності.

## 04. Створення макетів і сторінок

Скріншоти:

- [06-dashboard-page](./screenshots/08-dashboard-page.png)
- [07-folder-structure](./screenshots/07-folder-structure.png)

Що було зроблено:

- Було пройдено четвертий розділ офіційного туторіалу Next.js — **Creating Layouts and Pages**.
- Ознайомилась зі структурою маршрутизації в папці `app`.
- Створено сторінки для dashboard-частини застосунку.
- Додано спільний layout для dashboard-сторінок.
- Перевірила роботу нових сторінок у браузері через локальний сервер.

Спостереження:

- У Next.js маршрути створюються на основі структури папок у директорії `app`.
- Файл `page.tsx` відповідає за окрему сторінку маршруту.
- Файл `layout.tsx` дозволяє створити спільну структуру для кількох сторінок.
- Це зручно, тому що повторювані елементи інтерфейсу можна винести в один layout, а не дублювати на кожній сторінці.
- App Router робить структуру проєкту більш зрозумілою, оскільки маршрути напряму пов’язані з папками.

Питання та роздуми:

- Потрібно краще розібратися, коли варто створювати окремий `layout.tsx`, а коли достатньо звичайного компонента.
- Цікаво, як Next.js обробляє вкладені layouts у великих застосунках.
- Мені здається зручним, що структура URL формується через папки, бо це робить навігацію в проєкті більш логічною.

## 05. Навігація між сторінками

Скріншоти:

- [08-navigation-browser](./screenshots/08-navigation-browser.png)

Що було зроблено:

- Було пройдено п’ятий розділ офіційного туторіалу Next.js — **Navigating Between Pages**.
- Ознайомилась з навігацією між сторінками у Next.js.
- Використано компонент `Link` з `next/link` для переходів між сторінками без повного перезавантаження сторінки.
- Додано або перевірено навігаційні посилання в dashboard-частині застосунку.
- Перевірила роботу переходів між сторінками у браузері.
- Ознайомилась із визначенням активного посилання за допомогою `usePathname`.

Спостереження:

- Компонент `Link` у Next.js зручніший за звичайний тег `<a>`, тому що дозволяє переходити між сторінками швидше.
- Навігація працює без повного перезавантаження сторінки, що робить застосунок більш схожим на SPA.
- `usePathname` дозволяє отримати поточний шлях і використовувати його для підсвічування активного пункту меню.
- Для використання hooks, таких як `usePathname`, компонент має бути client component, тому потрібно додавати `"use client"`.

Питання та роздуми:

- Потрібно краще розібратися, коли компонент має бути server component, а коли client component.
- Цікаво, як Next.js оптимізує переходи між сторінками.
- Мені здається корисним підсвічування активного пункту меню, бо це покращує зручність користування застосунком.

## 06. Налаштування бази даних

Скріншоти:

- [09-database-setup](./screenshots/11-chapter-6-database-setup.png)
- [10-seed-console-browser](./screenshots/10-seed-console-browser.png)

Що було зроблено:

- Було пройдено шостий розділ офіційного туторіалу Next.js — **Setting Up Your Database**.
- Налаштовано підключення до PostgreSQL бази даних.
- Додано змінні середовища для підключення до бази даних.
- Запущено seed-скрипт через маршрут `/seed`.
- Перевірено, що початкові дані були додані до бази даних або що необхідні таблиці вже існують.

Спостереження:

- База даних використовується для збереження інформації про користувачів, клієнтів, рахунки та доходи.
- Seed-скрипт потрібен для початкового заповнення бази тестовими даними.
- Повідомлення типу `already exists, skipping` не є критичною помилкою. Воно означає, що таблиця або розширення вже були створені раніше.
- Змінні середовища потрібно зберігати у файлі `.env.local`, але не можна додавати цей файл у GitHub, тому що він може містити секретні дані.
- Під час роботи з PostgreSQL може виникати помилка, пов’язана з prepared statements, яку можна виправити через налаштування підключення.

Питання та роздуми:

- Потрібно краще розібратися, чим seed-скрипт відрізняється від міграцій бази даних.
- Цікаво, як Next.js безпечно працює з базою даних на серверній стороні.
- Також варто краще зрозуміти, як правильно організовувати `.env.local` та `.env.example` у реальних проєктах.

## 07. Отримання даних

Скріншоти:

- [11-fetching-data-code](./screenshots/14-chapter-7-fetching-data-code.png)
- [12-dashboard-data-browser](./screenshots/15-chapter-7-dashboard-data-browser.png)

Що було зроблено:

- Було пройдено сьомий розділ офіційного туторіалу Next.js — **Fetching Data**.
- Ознайомилась із тим, як отримувати дані з бази даних у Next.js.
- Переглянула функції для запитів до бази даних у файлі `app/lib/data.ts`.
- Підключила дані до сторінки dashboard.
- Перевірила, що на сторінці відображаються дані з бази, а не просто статична інформація.

Спостереження:

- У Next.js можна отримувати дані безпосередньо в server components.
- Це зручно, тому що не потрібно окремо створювати API-запит для кожного випадку отримання даних.
- Дані з бази можна використовувати прямо під час рендерингу сторінки.
- Server components допомагають не передавати зайвий JavaScript у браузер користувача.
- Робота з базою даних відбувається на серверній стороні, тому чутливі дані не потрапляють напряму в клієнтський код.

Питання та роздуми:

- Потрібно краще розібратися, коли краще отримувати дані напряму в server component, а коли створювати окремий API route.
- Цікаво, як Next.js кешує запити до бази даних.
- Також варто зрозуміти, як правильно обробляти ситуації, коли база даних тимчасово недоступна.

## 08. Статичний і динамічний рендеринг

Скріншоти:

- [13-rendering-code](./screenshots/13-rendering-code.png)

Що було зроблено:

- Було пройдено восьмий розділ офіційного туторіалу Next.js — **Static and Dynamic Rendering**.
- Ознайомилась із різницею між статичним і динамічним рендерингом.
- Перевірила, як Next.js обробляє сторінки, які використовують дані з бази даних.
- Розглянула ситуацію, коли один повільний запит може затримувати завантаження всієї сторінки.
- Перевірила роботу dashboard-сторінки після підключення динамічних даних.

Спостереження:

- Статичний рендеринг підходить для сторінок, дані на яких змінюються нечасто.
- Динамічний рендеринг потрібен тоді, коли сторінка залежить від актуальних даних, користувача або запитів до бази даних.
- Якщо сторінка очікує повільний запит до бази даних, користувач може довше бачити порожню сторінку.
- У Next.js важливо розуміти, які частини застосунку можуть бути статичними, а які повинні оновлюватися динамічно.
- Ця глава показала, чому оптимізація завантаження даних важлива для зручності користувача.

Питання та роздуми:

- Потрібно краще розібратися, як Next.js сам визначає, чи сторінка має бути статичною або динамічною.
- Цікаво, як правильно балансувати між швидкістю статичного рендерингу і актуальністю динамічних даних.
- Мені здається важливим вміти розділяти сторінку на частини, щоб повільні дані не блокували весь інтерфейс.

## 09. Потокове завантаження

Скріншоти:

- [14-skelton-loading](./screenshots/14-skelton-loading.png)
- [15-suspense-code](./screenshots/15-suspense-code.png)

Що було зроблено:

- Було пройдено дев’ятий розділ офіційного туторіалу Next.js — **Streaming**.
- Ознайомилась із тим, як Next.js дозволяє завантажувати сторінку частинами.
- Було додано `loading.tsx` для показу стану завантаження.
- Використано компонент `Suspense` для окремого завантаження частин dashboard.
- Картки, графік доходів та список останніх рахунків були розділені на окремі блоки завантаження.
- Перевірила, як skeleton-компоненти відображаються під час очікування даних.

Спостереження:

- Streaming допомагає не блокувати всю сторінку, якщо одна частина даних завантажується повільніше.
- `loading.tsx` автоматично показується під час завантаження маршруту.
- `Suspense` дозволяє створити більш гнучке завантаження окремих компонентів.
- Skeleton-компоненти покращують користувацький досвід, бо користувач бачить структуру сторінки ще до повного завантаження даних.
- Такий підхід особливо корисний для dashboard-сторінок, де різні блоки можуть отримувати дані з різною швидкістю.

Питання та роздуми:

- Потрібно краще розібратися, коли краще використовувати `loading.tsx`, а коли `Suspense`.
- Цікаво, як streaming працює на production-сервері та чи залежить це від хостингу.
- Мені здається корисним розділяти сторінку на незалежні блоки, щоб повільний запит не затримував увесь інтерфейс.

## 10. Пошук і пагінація

Скріншоти:

- [16-user-search-input](./screenshots/16-user-search-input.png)
- [17-updated-url](./screenshots/17-updated-url.png)
- [18-debouncing](./screenshots/18-debouncing.png)
- [19-pagination](./screenshots/19-pagination.png)

Що було зроблено:

- Було пройдено десятий розділ офіційного туторіалу Next.js — **Adding Search and Pagination**.
- Додано поле пошуку на сторінку invoices.
- Реалізовано оновлення URL під час введення пошукового запиту.
- Перевірено, що пошуковий запит зберігається в адресному рядку через параметр `query`.
- Додано debounce для оптимізації пошуку, щоб запит не виконувався після кожної введеної літери одразу.
- Додано пагінацію для таблиці invoices.
- Перевірено, що сторінки таблиці можна перемикати через елементи пагінації.

Спостереження:

- Зберігання пошуку в URL є зручним, тому що сторінку можна оновити або надіслати іншій людині без втрати поточного запиту.
- `useSearchParams` дозволяє читати параметри з URL.
- `usePathname` допомагає отримати поточний шлях сторінки.
- `useRouter` використовується для оновлення URL без повного перезавантаження сторінки.
- Debouncing допомагає зменшити кількість запитів і покращити продуктивність.
- Пагінація потрібна для того, щоб не завантажувати всі записи одразу, а показувати їх частинами.

Питання та роздуми:

- Потрібно краще розібратися, яку затримку debounce краще обирати для реального проєкту.
- Цікаво, коли краще зберігати стан пошуку в URL, а коли у локальному стані компонента.
- Мені здається корисним, що пошук і пагінація пов’язані з URL, бо це робить застосунок більш зручним для користувача.

## 11. Зміна даних

Скріншоти:

- [20-creating-invoice](./screenshots/20-creating-invoice.png)
- [21-showing-new-invoice](./screenshots/21-showing-new-invoice.png)
- [22-edited-invoice](./screenshots/22-edited-invoice.png)
- [23-deleted-invoice](./screenshots/23-deleted-invoice.png)

Що було зроблено:

- Було пройдено одинадцятий розділ офіційного туторіалу Next.js — **Mutating Data**.
- Додано можливість створення нового invoice через форму.
- Реалізовано збереження даних у базу даних за допомогою Server Actions.
- Перевірено, що після створення новий invoice з’являється у списку.
- Додано можливість редагування існуючого invoice.
- Перевірено, що зміни після редагування відображаються на сторінці.
- Додано можливість видалення invoice.
- Виправлено роботу кнопки Delete через прив’язку `id` до Server Action за допомогою `bind`.

Спостереження:

- Server Actions дозволяють змінювати дані на сервері без створення окремого API route.
- Для роботи з формами можна використовувати `FormData`.
- Після створення, редагування або видалення даних потрібно оновлювати шлях за допомогою `revalidatePath`.
- `redirect` використовується для повернення користувача на сторінку invoices після створення або редагування запису.
- Для видалення invoice важливо правильно передати `id` у Server Action.

Питання та роздуми:

- Потрібно краще розібратися, коли краще використовувати Server Actions, а коли API routes.
- Цікаво, як правильно показувати користувачу повідомлення про успішне створення, редагування або видалення запису.
- Мені здається, що Server Actions роблять роботу з формами зручнішою, але потрібно уважно слідкувати, які частини коду виконуються на сервері.

## 12. Обробка помилок

Скріншоти:

- [24-errors](./screenshots/24-errors.png)
- [25-404-page](./screenshots/25-404-page.png)

Що було зроблено:

- Було пройдено дванадцятий розділ офіційного туторіалу Next.js — **Handling Errors**.
- Додано обробку помилок для dashboard-частини застосунку.
- Створено файл `error.tsx`, який показує повідомлення у випадку помилки.
- Створено сторінку `not-found.tsx` для ситуації, коли потрібний invoice не знайдено.
- Перевірено роботу сторінки помилки.
- Перевірено роботу 404-сторінки через неіснуючий, але валідний `id`.

Спостереження:

- `error.tsx` використовується для обробки помилок, які виникають під час рендерингу або виконання коду.
- `not-found.tsx` використовується тоді, коли сторінка або конкретні дані не знайдені.
- Для виклику сторінки 404 у коді використовується функція `notFound()` з `next/navigation`.
- Якщо передати неправильний формат `id`, може спрацювати не 404-сторінка, а сторінка помилки, тому важливо розуміти різницю між “дані не знайдено” і “сталася помилка”.

Питання та роздуми:

- Потрібно краще розібратися, як правильно розділяти звичайні помилки і ситуації `not found`.
- Цікаво, як краще показувати користувачу зрозуміле повідомлення про помилку в реальному проєкті.
- Мені здається корисним, що Next.js дозволяє створювати окремі сторінки для помилок на рівні конкретного маршруту.

## 13. Покращення доступності

Скріншоти:

- [26-lint-error](./screenshots/26-lint-error.png)
- [27-lint-success](./screenshots/27-lint-success.png)
- [28-form-validation-errors](./screenshots/28-form-validation-errors.png)

Що було зроблено:

- Було пройдено тринадцятий розділ офіційного туторіалу Next.js — **Improving Accessibility**.
- Запущено перевірку коду за допомогою команди `pnpm lint`.
- Виправлено проблему з розташуванням файлу `eslint.config.mjs`.
- Налаштовано коректну роботу ESLint у директорії проєкту.
- Додано серверну валідацію форми створення invoice.
- Використано `useActionState` для обробки стану форми.
- Додано повідомлення про помилки для полів `customerId`, `amount` та `status`.
- Додано атрибути `aria-describedby`, `aria-live` та `aria-atomic` для покращення доступності форми.

Спостереження:

- Accessibility важлива для того, щоб застосунком могли зручно користуватися люди з різними потребами.
- ESLint допомагає знаходити не тільки помилки в коді, а й потенційні проблеми з доступністю.
- Серверна валідація є важливою, тому що не можна покладатися тільки на перевірку в браузері.
- `aria-describedby` дозволяє пов’язати поле форми з повідомленням про помилку.
- `aria-live` допомагає повідомляти assistive technologies про зміни на сторінці, наприклад появу помилки після відправлення форми.

Питання та роздуми:

- Потрібно краще розібратися, як тестувати accessibility у реальних проєктах.
- Цікаво, які ще інструменти, крім ESLint, можна використовувати для перевірки доступності.
- Мені здається важливим додавати повідомлення про помилки так, щоб вони були зрозумілі не тільки візуально, а й для screen readers.

## 14. Додавання автентифікації

Скріншоти:

- [29-login-page](./screenshots/29-login-page.png)
- [30-successful-login-dashboard](./screenshots/30-successful-login-dashboard.png)

Що було зроблено:

- Було пройдено чотирнадцятий розділ офіційного туторіалу Next.js — **Adding Authentication**.
- Додано сторінку входу `/login`.
- Налаштовано автентифікацію користувача через Auth.js / NextAuth.
- Додано файл `auth.ts` для налаштування входу через credentials.
- Додано `auth.config.ts` для опису правил доступу до захищених маршрутів.
- Створено `proxy.ts` для перевірки доступу до dashboard-сторінок.
- Додано Server Action `authenticate` для обробки форми входу.
- Перевірено, що користувач може увійти в систему і після успішного входу потрапляє на dashboard.

Спостереження:

- Автентифікація потрібна для захисту сторінок, які не мають бути доступні неавторизованим користувачам.
- `AUTH_SECRET` використовується для безпечної роботи сесій.
- Login form використовує server action для перевірки даних користувача.
- Захищені маршрути допомагають автоматично перенаправляти користувача на сторінку входу, якщо він не авторизований.
- Секретні ключі потрібно зберігати у `.env` або `.env.local` і не додавати їх у GitHub.

Питання та роздуми:

- Потрібно краще розібратися, як саме працюють сесії в Auth.js / NextAuth.
- Цікаво, як у реальному проєкті краще реалізовувати реєстрацію нового користувача.
- Також важливо зрозуміти, як правильно зберігати паролі та чому вони мають бути захешовані.
